apiVersion: eventing.knative.dev/v1alpha1
kind: Trigger
metadata:
  name: t
spec:
  filter: {}
  subscriber:
    ref:
     apiVersion: serving.knative.dev/v1alpha1
     kind: Service
     name: message-dumper

