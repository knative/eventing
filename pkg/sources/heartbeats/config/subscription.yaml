apiVersion: eventing.knative.dev/v1alpha1
kind: Subscription
metadata:
  name: heartbeats
  namespace: default
spec:
  from:
    kind: Source
    apiVersion: eventing.knative.dev/v1alpha1
    name: heartbeats
  call:
    target:
      kind: Service
      apiVersion: serving.knative.dev/v1alpha1
      name: message-dumper
