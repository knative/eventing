# logLevel = 'DEBUG'
observabilityConfig = '{{- .ObservabilityConfig -}}'
[sender]
address = '{{- .Endpoint -}}'
interval = {{ .Config.Interval.Nanoseconds }}
[forwarder]
target = '{{- .ForwarderTarget -}}'
